v1.7.1 CACHE-BUST FIX

The corrected wager data IS already in the GitHub repository, but index.html was still loading:
  data.js?v=1.7

That can cause the browser/CDN to keep serving the older cached data.js.

Upload these THREE files to the ROOT of the repository and replace the existing versions:
  1. index.html
  2. 404.html
  3. data.js

The new index/404 load data.js?v=1.7.1 and also bump all v1.7 assets to v1.7.1, forcing a fresh fetch.

Expected wager changes:
- Western Michigan / Michigan OVER 49.5 — LOSS (-$55)
- Notre Dame -21 — $220 risk, WIN (+$200)
- Notre Dame Team Total OVER 33.5 — $120 risk, WIN (+$100)
- Week 1 total booked exposure = $952 / 9.52u
- Weekly cap remaining = $48 / 0.48u

After GitHub Pages finishes deploying, hard refresh once (Ctrl+Shift+R) if needed.
